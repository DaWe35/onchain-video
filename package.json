{
  "name": "onchain-video",
  "version": "1.0.0",
  "description": "Upload and watch videos on the blockchain",
  "scripts": {
    "start": "node public_html/server.js",
    "dev": "cd public_html && serve -l 5000 --cors"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
